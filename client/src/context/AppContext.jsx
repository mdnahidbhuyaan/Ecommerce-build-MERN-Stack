import { createContext } from "react";


const AppContext = createContext(null)

const AppContextProvider = ({children})=>{
  return 
    const value = {}
    <AppContext.Provier value={value}>
      {children}
    </AppContext.Provier>
  
}

export default AppContextProvider